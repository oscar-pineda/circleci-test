---
version: 2.1

jobs:
  build:
    docker:
      - image: alpine
    steps:
      - checkout
      - run:
          name: print HelloWorld
          command: |
            echo 'Hello World'

workflows:
  build-n-deploy:
    jobs:
      - build
...

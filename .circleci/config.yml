---
version: 2.0

jobs:
  test:
    docker:
      - image: alpine
    steps:
      - checkout
      - run:
          name: print HelloWorld
          command: |
            echo 'Hello World'

workflows:
  build-n-deploy:
    jobs:
      - test
...
